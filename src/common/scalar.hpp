#pragma once

#include <cstddef>

namespace math { namespace scalar {

void VectorMultiply(float* C,
                    float* A,
                    float* B,
                    const size_t len);

} // namespace scalar
} // namespace math

